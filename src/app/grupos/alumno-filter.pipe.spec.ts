import { AlumnoFilterPipe } from './alumno-filter.pipe';

describe('AlumnoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AlumnoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
